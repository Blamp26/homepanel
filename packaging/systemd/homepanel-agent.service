[Unit]
Description=HomePanel Terminal Agent
After=network.target

[Service]
User=homepanel
Group=homepanel
ExecStart=/usr/bin/homepanel-agent --config /etc/homepanel/config.toml
Restart=on-failure
WorkingDirectory=/var/lib/homepanel

[Install]
WantedBy=multi-user.target
